# Copy to .env for local ingest runs. In CI these come from GitHub Actions secrets/vars.
GEMINI_API_KEY=your-key-here
# Optional override; otherwise the model in config/sources.json is used.
GEMINI_MODEL=gemini-2.0-flash
